# Reserved Package Routes

This directory is reserved for future package-owned WebBlocks CMS route files.

During the package transition, active root `routes/` remains authoritative unless routes are intentionally moved and wired in a focused migration phase.
