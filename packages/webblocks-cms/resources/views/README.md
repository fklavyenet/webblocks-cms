# Reserved Package Views

This directory is reserved for future package-owned WebBlocks CMS views.

The package view namespace may be registered for boundary testing, but active root `resources/views/` remains authoritative until views are intentionally moved and wired.
