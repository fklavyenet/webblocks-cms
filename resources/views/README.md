# Package Views

This directory is the package boundary for package-owned WebBlocks CMS views.

The package view namespace is active for package-owned CMS views and first-party plugin views. Active root `resources/views/` remains authoritative only for root-owned surfaces until views are intentionally moved and wired.
