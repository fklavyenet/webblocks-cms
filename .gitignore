/vendor/
/composer.lock
/.phpunit.cache/
/.phpunit.result.cache
/coverage/
/storage/
.DS_Store
**/.DS_Store

# Local AI agent contract — never tracked or pushed
AGENTS.md

# Local AI agent tooling config (launch.json for the preview runner) — same
# rule as AGENTS.md: it describes this checkout, not the package.
/.claude/
